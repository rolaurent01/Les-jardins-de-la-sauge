/**
 * Types générés depuis le schéma Supabase.
 * Ce fichier sera remplacé par les types auto-générés via `supabase gen types typescript`
 * une fois le schéma SQL créé (Phase A0 — migrations).
 *
 * Pour régénérer : npx supabase gen types typescript --project-id <project-id> > src/lib/supabase/types.ts
 */
export type Database = {
  public: {
    Tables: Record<string, never>
    Views: Record<string, never>
    Functions: Record<string, never>
    Enums: Record<string, never>
  }
}
